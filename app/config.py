KAFKA_BROKER = 'kafka://kafka:9092'

TOPICS = {
    'messages': 'messages',
    'filtered_messages': 'filtered_messages',
    'blocked_users': 'blocked_users',
    'censored_messages': 'censored_messages'
}
